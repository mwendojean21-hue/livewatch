# Laisser vide, y compris en production : sur Vercel (Services), frontend et
# backend partagent un seul domaine, donc les appels relatifs à /api
# fonctionnent tels quels (voir vercel.json à la racine du dépôt).
# En développement local, /api est relayé vers http://localhost:8001 par le
# proxy Vite (voir vite.config.ts).
VITE_API_BASE_URL=
